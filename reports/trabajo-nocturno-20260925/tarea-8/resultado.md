# Tarea 8 — investigación y decisión previa a cualquier DDL

**No ejecutar DDL de Tarea 8.** Los dos índices *seleccionados* para corte y reporte de crédito ya se instalaron el 24-sep-2026, y el catálogo consultado hoy los muestra válidos y listos. No existe un índice pendiente de esos dos que ensayar o aplicar de nuevo. Esta comprobación no es una nueva autorización para escribir en la base de la aplicación.

## Fuentes y comprobación de solo lectura

- Las dos instrucciones exactas están en `reports/tanda-h/tarea-2/approved-indexes.sql`, seleccionadas de `reports/tanda-g-ampliada/tarea-4/proposed-indexes.sql`: `tickets_pendientes_corte_ga_candidate` sobre `tickets (ubicacion_id, created_at, id)` y predicado de pendientes de corte; `tickets_contabilizados_sitio_fecha_ga_candidate` sobre `(ubicacion_id, CASE WHEN documento_tipo = 'TICKET' THEN cobrado_at ELSE autorizado_at END)` y predicado de contabilizados.
- `reports/tanda-h/continuacion/application/application-result.json` documenta ambas operaciones `created_concurrently`, estado `applied_verified`, validación de catálogo y comparación de la copia; `reports/tanda-h/continuacion/release/runtime-verification.md` confirma que ambas precedieron al reinicio. **Ya se ensayaron en copia** según `reports/tanda-h/continuacion/setup/copy-indexes.json` y la evidencia emparejada referenciada por el resultado de aplicación. No se repite un ensayo de DDL sin DDL nuevo.
- Consulta exclusivamente SELECT mediante `executeSql` aprobado, entorno development: `current_database() = heliumdb`, `server_version_num = 160010`. `pg_class`/`pg_index`/`pg_get_indexdef` para ambas relaciones: exactamente los dos índices seleccionados presentes en `public.tickets`, `indisvalid = true`, `indisready = true`, columnas/expresión/predicados iguales a los aprobados tras normalización de tipos por PostgreSQL. La consulta también buscó `movimientos_credito_cliente_fecha_id_ga_candidate` en `public.movimientos_credito`: **no está presente**; ese tercer candidato, para una cuenta larga, nunca fue uno de los dos índices seleccionados ni quedó aprobado para aplicación. No se ha inferido identidad de servidor exclusivamente por nombre de base; cotejar el proceso efectivo antes de cualquier futura escritura, a cargo de MAIN.

## Medición existente con carga anual sintética, no nueva certificación

La copia de `reports/tanda-h/continuacion/setup/load-results.json` contenía 54,750 tickets sintéticos añadidos (3 tiendas × 50 diarios × 365 días), 54,753 tickets totales y 13,691 movimientos de crédito. Concentró la carga en un cliente y cuatro productos: **supuesto de estrés, no volumen real**. Los índices estaban creados en esa copia antes de la navegación. El harness de navegador existente es `reports/tanda-h/continuacion/mediciones/browser.mjs`, con resultados resumidos en `mediciones/summary.json`; no se corrió el navegador sobre la base de aplicación. **Importante:** el reporte en navegador utiliza el filtro mensual predeterminado sobre una base cargada con un año; no demuestra que se haya elegido un rango anual en la pantalla (véase `reports/tanda-g-ampliada/tarea-4/browser-informe.md`). La medición anual del *rango visible* sigue pendiente.

| Operación y definición medida en copia | Resultado disponible | Objetivo / estado |
|---|---|---|
| Reporte clientes/crédito, ruta hasta `report-content-clientes` + dos frames | 11,005.31 y 9,804.43 ms, dos muestras listas | **No cumple <3 s**; falta tercera muestra. Aproximadamente 261,710 nodos DOM y 8.91–9.99 s de tarea principal; HTTP TTFB 825/758 ms, que no es duración SQL. |
| Corte, abrir diálogo y ver `Efectivo Esperado` | Navegación hasta diálogo 4,653.47 ms; desde clic `Realizar Corte` hasta diálogo 945.42 ms | <2 s **solo para el clic** en una muestra; navegación completa no cumple. Faltan tres muestras listas. |
| Estado de cuenta E7, cliente sintético grande | Dos intentos sin `e7-client-export` listo antes de timeout | **Sin tiempo válido**. Cliente pequeño: clic de pestaña hasta listo 865.62 ms, no sustituye cuenta grande. |
| Cobro efectivo nativo, tres tickets de copia | Desde `Confirmar Pago` hasta éxito 1,166.51 / 319.37 / 396.30 ms; ruta previa 2,916.94 / 2,491.53 / 2,211.22 ms | Clic válido en copia; ruta completa supera 2 s. Solo efectivo, no transferencia. |

**Diagnóstico acotado:** más DDL no soluciona el costo dominante observado del reporte: montar ~261 mil nodos en el navegador. Hace falta paginar/virtualizar la tabla conservando los mismos datos y significado, y volver a medir. No atribuir duración DOM a SQL. Estado de cuenta grande requiere primero separar descarga/consulta E7, CPU/render y timeout; no inventar una consulta culpable. El índice adicional de `movimientos_credito` sigue siendo una **propuesta no aprobada**: solo ante evidencia de plan `EXPLAIN (ANALYZE, BUFFERS)` de esa consulta exacta y demostración antes/después en copia se plantearía DDL mínimo a MAIN, sin aplicarlo ahora.

## Límite de esta intervención

No se creó sesión de aplicación, no se escribieron filas, no se ejecutó DDL ni se arrancó/reinició workflow o app; no se cambiaron consultas ni archivos de finanzas compartidos con otros trabajadores. La copia privada anterior fue destruida (`setup/teardown-final.json`: puertos y raíz privada eliminados); por ello no hay medición **nueva** ni ensayo reproducido en este tramo. Las metas de crédito y corte no están certificadas como liberadas. Si MAIN decide repetir mediciones, necesitará nueva copia aislada y sus propios servidores en puertos dedicados, salud `/api/healthz` 200 y sesiones sintéticas exclusivamente de la copia; jamás trasladar sesiones ni escrituras al servidor de la aplicación.