# Tarea 4 — etiqueta de rollo

**Alcance:** únicamente `label-print.tsx`, sus pruebas y este informe. No hubo escritura ni consulta nueva a la base, autenticación, reinicio ni commit (pendiente de coordinación de MAIN). MAIN autorizó posteriormente el typecheck y un build candidato independiente; no se alteró el `dist/public` servido.

**Fuente del catálogo:** exportación de solo lectura preexistente `reports/prompt-l/full-products-readonly-export.json` (1234 productos, documentada previamente como `REPEATABLE READ READ ONLY`). El ensayo visual aislado escogió los 20 nombres más largos de esa exportación; **no equivale a comprobar las 1234 etiquetas**, ni acredita estado actual de la base. Longitudes máximas: 42 caracteres (`Mezclilla Diamantina 10 Oz - Indigo Rayada`) y 41 (`Campesina Flor Margarita - Amarillo Mango`).

**Cambio:** el encabezado mantiene 11 mm dentro de la etiqueta 100 × 70 mm. El nombre puede ocupar dos renglones completos, con cada palabra no divisible; las mediciones comprueban ancho de la palabra más larga, altura máxima de dos renglones y espacio de encabezado. SKU y serie se ajustan con escalones legibles sin `truncate`. Si no cabe un campo al mínimo, la etiqueta se cubre con un aviso blanco de error y valor completo, y `beforeprint` pide cancelar y anuncia el error. Algunos navegadores no respetan la cancelación de `beforeprint`; en ese caso se imprimiría **la página de error, no la etiqueta ambigua**. No se modificaron cuerpo, columnas, logo, QR ni las medidas físicas. Impresión física Wasp pendiente.

**Evidencia:** `before-label.png` / `after-label.png` son capturas Chromium del componente React real renderizado en archivo `file://` aislado, con CSS compilado del candidato y reglas aisladas de distribución; `before-geometry.json` / `after-geometry.json` guardan tamaños y estados. El segundo nombre largo pasó de recorte horizontal a dos líneas; en 20/20 nombres largos de la exportación no hubo bloqueo, el QR midió 109.59 px (29 mm a 96 dpi), y cada página midió 377.94 × 264.56 px (100 × 70 mm). Los dos valores sintéticos imposibles (una palabra excesivamente larga y una serie excesivamente larga) muestran error explícito con valor íntegro. El encabezado no invadió el cuerpo.

**Medio impreso:** `after-two-labels-print.pdf` es un PDF real de Chromium con `@media print` y exclusivamente dos etiquetas **válidas**; `pdfinfo`: 2 páginas de 282.96 × 198 pt (nominal 100 × 70 mm, redondeo Chromium). El texto extraído conserva SKU, serie de 8 cifras y todas las palabras del nombre partido; `after-print-page-2.png` es la segunda página rasterizada a 144 dpi, con cuerpo, logo y QR conservados. El arnés añade únicamente reglas de aislamiento para que dos wrappers de prueba paginen sin hojas extras: esto **no acredita el controlador Wasp ni el flujo autenticado de la aplicación**.

**Pruebas:** `node scripts/src/frontend-test-runner.mjs src/components/label-print.contract.test.ts`: 4/4. `node artifacts/mariana-textil/src/components/label-print.offline.test.mjs`: 22/22 con 20 productos reales + dos negativos sintéticos; `positive.log`. Mutación controlada `LABEL_TEST_MUTANT=no-wrap` desactiva el salto de línea y hace fallar la prueba del segundo nombre largo (salida 1; `negative-no-wrap.log`). La prueba antigua también habría fallado porque no había elemento/ajuste para la serie.

**Candidato:** `pnpm --filter @workspace/mariana-textil run typecheck` terminó en 0 y `pnpm --filter @workspace/mariana-textil exec vite build --config vite.config.ts --outDir dist-night-task4` generó `artifacts/mariana-textil/dist-night-task4/` sin reemplazar `dist/public`, sin arrancar servidor ni reiniciar workflow. El arnés offline usa su CSS `index-DqBN0ddH.css`. MAIN decide cuándo servirlo y ejecutar sus verificaciones runtime.

**Identidad exacta SHA-256:** fuente `label-print.tsx`: `84f7b55a1d570a8e0c11c66a330794a66a3d6416d6e78a98c63e505c70a2a531`; exportación de catálogo: `97c5087799c55ebf422a1aa514fa0a442624749ab247a94a9532080e83b28ca2`; CSS candidato `index-DqBN0ddH.css`: `ad36b19a0ace21c10585249d55fe27a870e1d201f4c62ff44c83c083bca94dde`; JS candidato `index-DvjqkTbt.js`: `dcee698249b63ea80c8afc34febbc2bdcfd855a3270e0ac1fb1b6056f89eb454`.

**Límites:** no se hizo barrido geométrico de las 1234 filas; MAIN autorizó acotar al ensayo de los 20 nombres más largos. No se verificó el diálogo de impresión de tienda ni Wasp. Una etiqueta inválida activa el aviso modal `beforeprint`; por ello el PDF desatendido usa solo casos válidos. No hay botón de impresión dentro de `LabelPrint`; impedir abrir el diálogo desde botones de las pantallas exigiría editar páginas fuera del alcance «solo encabezado/label-print.tsx». `preventDefault` no cancela de modo fiable los diálogos nativos; la seguridad adicional es cubrir cualquier etiqueta ambigua con página de error explícito, no prometer cancelación nativa. No hay decisión de negocio nueva.