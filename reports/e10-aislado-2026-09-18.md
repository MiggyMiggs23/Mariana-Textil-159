# E10 Fondo Mariana — verificación frontend enfocada corregida

## Correcciones a evidencia anterior

La evidencia anterior de este archivo no era válida: dos ejecuciones mutantes se habían reportado verdes sin ejercitar los defectos y la supuesta restauración verde contenía fallos reales de formato monetario. Esas afirmaciones quedan sustituidas íntegramente por los resultados medidos abajo.

No se abrió DB, no se crearon usuarios/sesiones, no se instalaron paquetes y no se iniciaron/reiniciaron workflows.

## Manifiesto seguro restaurado

`artifacts/mariana-textil/test-manifests/safe.txt` vuelve a contener exactamente las 100 entradas de `HEAD`, en el mismo orden, más una sola entrada nueva al final:

`src/components/fondo/fondo-summary.contract.test.ts`

El diff del manifiesto es únicamente esa adición (más terminador de línea).

## Qué verifica realmente el test

`fondo-summary.contract.test.ts` tiene seis subtests dentro de una suite:

1. Estado inicial sin `versionSaldo`.
2. Saldo inicializado y acciones visibles.
3. Retiro con body canónico y sin conciliación inicial.
4. Exportación realmente diferida: comienza como ADMIN, queda pendiente, el **mismo `QueryClient`** cambia a CONTADOR, se resuelve la respuesta y se comprueba error de transición, cero intentos de descarga y ausencia de éxito.
5. La misma caché de usuario de React Query cambia ADMIN→CONTADOR; el stub de Fondo ya no simula 403, de modo que el bloqueo observado procede del auth key/`enabled` real del hook.
6. Render exacto al centavo de strings por encima de precisión segura de `number`.

## Ejecución enfocada restaurada

Comando: runner frontend existente, un solo archivo, preload `scripts/src/offline-test-guard.cjs`, entorno hijo sin credenciales fuente y URL DB inválida.

Resultado real:

- **7 tests PASS, 0 FAIL** (seis subtests + suite padre).
- Duración Node: `2728.875382 ms`.
- Tiempo de pared: `2846 ms`.

## Mutaciones reales en árboles temporales

Los fuentes vigilados nunca se intercambiaron. Para cada caso se copió el artifact a un árbol temporal, se mutó el fuente de producto dentro de esa copia, se ejecutó el mismo test con preload offline y se eliminó el árbol.

| Mutante de producto | PASS | FAIL reportados por Node | pared | Resultado relevante |
|---|---:|---:|---:|---|
| `fondo-utils.ts`: interpolación monetaria vuelve a literales escapados | 4 | 3 | 22866 ms | RED en subtests 2 y 6; el tercero es la suite padre |
| `hooks/fondo.ts`: elimina revalidación de rol posterior al `await` de exportación | 5 | 2 | 2777 ms | RED en subtest 4; intentó descargar tras transición; segundo fallo es suite padre |
| `hooks/fondo.ts`: reemplaza usuario de QueryClient por ADMIN estático | 5 | 2 | 2857 ms | RED en subtest 5; saldo permaneció visible; segundo fallo es suite padre |

Total: **3/3 mutantes detectados** mediante fallos de aserción/condición funcional, sin error de parseo, importación ni infraestructura.

## Alcance pendiente

Esta fue sólo la prueba frontend nueva y sus mutantes. No se ejecutó la suite segura completa ni typecheck raíz; el padre conserva esas verificaciones finales.