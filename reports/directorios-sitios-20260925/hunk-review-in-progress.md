# Revisión de hunks — pendiente de congelar

Fuente inspeccionada: `git diff HEAD` sobre el árbol concurrente, contrastado con el sourcemap API **activo** y `.local/e7-text-baseline` (no con el contenido completo de HEAD). Este inventario **no autoriza build**. RFC y crédito siguen en edición; sus hashes finales y overlays aún no son estables.

## Hunk inventory para aprobación final

- API `routes/clientes.ts`: solo `GET /clientes/:id/compras`, SELECT añade `documento_tipo AS "documentoTipo"` y ORDER añade `NULLS LAST, t.id DESC`. HEAD base de este archivo **no coincide** con sourcemap retenido: contiene trabajo comercial ajeno; insertar únicamente esas dos sustituciones.
- Web `components/client-directory.tsx`: valor inicial `nombre/asc`; sustituir botones rápidos por selector A–Z / Mayor demanda y botón separado deuda. Mantener columna de última actividad y periodos. `pages/cliente-detail.tsx`: import `saleTypeLabel`, rótulo de pestaña y fila de tabla de compras con tipo de venta. HEAD base del detalle **no coincide** con E7 segura; el resto del archivo puede incluir devolución comercial, nunca copiarlo entero. `lib/clientes-api.ts`: tipo de documento y `saleTypeLabel` exclusivamente.
- Crédito `pages/clientes.tsx`: import tipo Location, bloqueo de selección Cartera que no es TIENDA, filtro de catálogo de tienda y guardas de petición/exportación. `components/credit-site-scope.tsx`: módulo nuevo solo para lectores locales; `components/e7-readers.tsx`: contexto de crédito en atribución y estado de cuenta; `pages/reportes.tsx`: parámetros, comparación y exportación locales solo para pestaña clientes. **Pendiente:** corrección de Cartera predeterminada y selector Cobros contextual prometidos por agente crédito; no pinnear hasta que entregue cambio y pruebas finales. `AppLayout` y `LocationScopeProvider` excluidos.
- Proveedor API `routes/proveedores.ts`: imports de contrato, GET `/proveedores/directorio`, proyección RFC nullable, creación/edición RFC; `lib/db/src/schema/proveedores.ts`: columna nullable. Web `pages/proveedores.tsx`: listado, conteo/periodo/orden, búsqueda nombre/contacto/teléfono/RFC, alta RFC; `pages/proveedor-detail.tsx`: lectura/edición RFC. OpenAPI: ruta, respuesta, campo nullable y generated: SOLO los hunks `ProveedoresDirectorio` y `rfc`; nunca el archivo generado completo de HEAD. **Pendiente:** agente proveedor finaliza revisión RFC/DDL. `lib/db/migrations/20260925_supplier_rfc.sql` es un candidato **no ejecutado**, no va en sourcemap/bundle.
- Contrato frontend: `lib/api-client-react/src/generated/api.ts` contiene import de 2 tipos y bloque del hook de directorio; `.schemas.ts` contiene RFC y tipos de directorio. Contrato backend: `lib/api-zod/src/generated/api.ts` contiene 8 validadores RFC/response + bloque directorio. Los índices y archivos `generated/types` contienen referencias a otras funcionalidades; requerir composición aditiva con verificación de imports y exclusión comercial.

Pruebas de frontend (`client-directory.test.tsx`, `clientes-api.contract.test.ts`, `credit-site-scope.contract.test.ts`, `proveedores-historial.contract.test.ts`) son material de revisión de fuente, no requieren copiarse al binario. SQL DDL debe ser autorizado y ejecutado por MAIN separadamente **antes** de activar cualquier API que lea/escriba `proveedores.rfc`; sin columna activa, la API nueva fallaría. No hay autorización de DB aquí.

## Bloqueos para un `approval.json` válido

Los cambios de crédito y proveedor todavía se editan; un SHA-256 escrito ahora podría anclar una versión incompleta o quedar obsoleto en segundos. Para cumplir la regla de overlay exclusivamente autorizado, el script exige archivos completos **compuestos contra el padre activo**, sus hashes exactos y `approval.json`. No se ha creado ninguno ni se ha compilado. Al quedar congeladas las tres entregas, producir overlays separados por hunk, verificar los hashes y entregar inventario final antes de que MAIN autorice el build.