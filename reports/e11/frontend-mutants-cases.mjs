// One exact productive semantic mutation per mounted obligation. Not execution evidence.
export const coverageStatus = "PREPARED_FINAL_21_HOOKS_153_MOUNTED_OBLIGATIONS_NOT_EXECUTED";
export const knownGreenBlockers = [];
export const hookCoverage = {
  useGetE11Disponibilidad: ["E11-DISABLED-NO-IDENTITY", "E11-RECOVERY-ADMIN-INDEPENDENT-GATES"],
  useGetE11Identidad: ["E11-IDENTITY-USER", "E11-IDENTITY-AUTH-REVOKED"],
  useGetE11Perfil: ["E11-PROFILE-ADMIN", "E11-USUARIOS-PARENT-ASSIGN"],
  useAssignE11Perfil: ["E11-PROFILE-ADMIN", "E11-PROFILE-RESPONSE-UUID"],
  useListE11PerfilHistorial: ["E11-PROFILE-HISTORY-ID", "E11-HISTORY-PAGE"],
  useListE11FiscalClientes: ["E11-F-CLIENTS-SALES", "E11-F-PAGER"],
  useListE11FiscalVentas: ["E11-F-SALES-CLIENT-ID", "E11-FISCAL-SALES-PAGE"],
  useGetE11FiscalFactura: ["E11-F-DOCUMENT", "E11-F-DOCUMENT-ID"],
  useListE11FinanzasClientes: ["E11-A-CLIENTS", "E11-FINANCE-CLIENTS-PAGE"],
  useListE11FinanzasNotas: ["E11-A-NOTES-UNINVOICED", "E11-FINANCE-NOTES-PAGE"],
  useGetE11FinanzasEstadoCuenta: ["E11-A-ACCOUNT-ID", "E11-FINANCE-ACCOUNT-PAGE"],
  useListE11Conciliaciones: ["E11-PERIODS-PAGE", "E11-ADMIN-NOTICE"],
  useCreateE11Conciliacion: ["E11-SNAPSHOT-SEND", "E11-SNAPSHOT-RESPONSE-PERIOD"],
  useGetE11Conciliacion: ["E11-SNAPSHOT-ID", "E11-DECISION-HISTORICAL"],
  useDecideE11Conciliacion: ["E11-DECISION-NO-CUADRA", "E11-DECISION-RESPONSE-ACTOR"],
  useListE11ConciliacionVentas: ["E11-SNAPSHOT-FROZEN", "E11-SNAPSHOT-SALES-PAGE"],
  useListE11Preparaciones: ["E11-PREPARATIONS", "E11-PREPARATIONS-PAGE"],
  useGetE11Preparacion: ["E11-PREPARATION-ID", "E11-PREPARATION-NOTES-SAME-CLIENT"],
  usePrepareE11Aplicacion: ["E11-PREPARATION-SEND", "E11-PREPARATION-EXACT-CHARGE-FRESH"],
  useGetE11OperacionRecuperacion: ["E11-RECOVERY-ORIGINAL-ACTOR", "E11-RECOVERY-GET-TOMBSTONE"],
  useResolveE11Operacion: ["E11-RECOVERY-POST-TERMINAL", "E11-RECOVERY-POST-REPLAY-EXACT"],
};
// Each final recovery obligation now has a literal mounted test and productive
// mutant below. Preparation does not claim observed runtime sensitivity.
export const recoveryObligations = [
  ["ORIGINAL-ACTOR", "GET/POST paths use actorId of the original owner (9), never current ADMIN (7)."],
  ["ORIGINAL-ACTION", "GET/POST paths retain the exact original action; no fallback action inferred from current page."],
  ["ORIGINAL-UUID", "GET/POST paths retain uuidOriginal; resolver UUID is a separate identity."],
  ["RELOAD", "Metadata-only original terna survives reload and allows server consultation without original A payload."],
  ["ADMIN-INDEPENDENT-GATES", "Real ADMIN with FISCAL_LEER can recover while perfiles/conciliacion/preparacionE5 are all false."],
  ["CAPABILITY", "ADMIN lacking FISCAL_LEER cannot read or resolve recovery."],
  ["NONADMIN", "CONTADOR F with FISCAL_LEER cannot read or resolve recovery."],
  ["OFF", "E11 OFF never sends recovery GET/POST and does not discard original metadata."],
  ["GET-PENDING", "PENDIENTE is absence observed, not proof of rollback; retain original terna and block business replacement."],
  ["GET-COMMITTED-UNAUDITED", "CONFIRMADA with null resolucionId does not release; explicit ADMIN resolution remains necessary."],
  ["GET-CONFIRMED", "Exact CONFIRMADA with authoritative resolution id/date can release that original terna."],
  ["GET-TOMBSTONE", "Exact CERRADA_SIN_EFECTO with authoritative resolution id/date can release without reissuing business."],
  ["GET-404", "404 NO_ENCONTRADO does not discard/quarantine-clear or infer rollback."],
  ["GET-ERROR", "Timeout/503/403 preserve original metadata; no optimistic local resolution."],
  ["RESPONSE-ACTOR", "Schema-valid foreign actorId cannot release or show a successful resolution."],
  ["RESPONSE-ACTION", "Schema-valid foreign accion cannot release or show a successful resolution."],
  ["RESPONSE-UUID", "Schema-valid foreign uuidOriginal cannot release or show a successful resolution."],
  ["RESPONSE-REVISION", "Invalid revision is not valid authoritative state and cannot feed a resolution request."],
  ["RESPONSE-RESOLUTION-ID", "Missing/malformed resolution identity never releases metadata."],
  ["RESPONSE-RESOLVED-AT", "Missing/invalid resolution timestamp never presents audited terminal completion."],
  ["EXACT-RELEASE", "Release only the exact actor/action/original-UUID; preserve other actors/actions even sharing original UUID."],
  ["POST-BODY", "Resolution sends only uuid/revisionEsperada/identidadVersion/motivo, no original A/business payload."],
  ["POST-UUID", "ADMIN resolution uses its own UUID, not uuidOriginal; no new business intention is synthesized."],
  ["POST-REASON", "Nonempty reviewed motive is required; do not silently fabricate it."],
  ["POST-FRESH-IDENTITY", "Revalidate real ADMIN/FISCAL_LEER and bind identidadVersion to fresh ADMIN permisosVersion before POST."],
  ["POST-FRESH-CAS", "Bind revisionEsperada to GET of the exact original terna; changed revision requires renewed review."],
  ["POST-DOUBLE", "Concurrent confirm clicks cannot submit duplicate resolver mutations."],
  ["POST-TERMINAL", "Only exact authoritative audited CONFIRMADA/CERRADA_SIN_EFECTO releases the original terna."],
  ["POST-PENDING", "Even a successful transport with PENDIENTE or unaudited CONFIRMADA cannot release."],
  ["POST-UNCERTAIN", "Lost resolution response preserves resolver UUID/body for authorized ADMIN and original terna."],
  ["POST-OUTCOME-ERRORS", "409 RESULTADO_CONFIRMADO_NO_CONSULTABLE and 503 RESULTADO_INCIERTO preserve resolver intent without financial disclosure."],
  ["POST-REPLAY-EXACT", "Authorized replay sends exactly the same resolver UUID/body to the same original-terna path."],
  ["POST-REPLAY-403", "Replay rejection does not prove original resolution rollback; preserve recovery and require authority."],
  ["POST-CAS-PENDING", "REVISION_OBSOLETA triggers fresh exact GET; PENDIENTE does not release or silently create another resolver UUID."],
  ["POST-CAS-TERMINAL", "After REVISION_OBSOLETA, exact authoritative audited terminal GET can release original recovery."],
  ["POST-TIMEOUT-GET-CONFIRMED", "After lost POST response, authoritative exact GET CONFIRMADA releases without replaying money."],
  ["POST-TIMEOUT-GET-TOMBSTONE", "After lost POST response, authoritative exact GET CERRADA_SIN_EFECTO releases without business retry."],
  ["NO-RECURSIVE-QUARANTINE", "Resolver UUID is never quarantined as a new original business operation; continue consulting original terna."],
  ["ADMIN-SESSION-LOSS", "Loss of ADMIN authorization removes resolver body/private details, preserves only original recovery metadata."],
  ["SECOND-ADMIN", "A different authorized ADMIN can consult/resolve the original owner namespace without old resolver/business body."],
  ["LEGACY-MARKER-NO-GUESS", "Marker missing original actor/action/UUID is blocked explicitly, not assigned current ADMIN or discarded as resolved."],
  ["NO-BUSINESS-RESEND", "Recovery GET/resolution only; never call original profile/snapshot/decision/preparation mutations to unblock."],
].map(([id, obligation]) => ({ id: `E11-RECOVERY-${id}`, obligation, status: "MOUNTED_PREPARED_NOT_EXECUTED" }));
export const pendingRecoveryObligations = [];
const page = "src/pages/e11.tsx", session = "src/lib/e11-session.tsx", write = "src/hooks/use-e11-write.ts";
const recovery = "src/lib/e11-recovery.ts", resolver = "src/components/e11-recovery-admin.tsx";
const discardMarker = 'localStorage.removeItem(`e11-recovery:${record.actorId}:${record.accion}:${record.uuidOriginal}`);';
const resolverCatch = 'setError(`${(e as Error).message}. Conserva UUID resolutor;';
const catchConsult = "try { await consult(); } catch { /* Prior explicit error remains; original marker stays. */ }";
const c = (id, file, before, after, title) => ({ id: `E11-${id}`, assertion: `E11-${id}`, file, before, after, title: title ?? id });
export const cases = [
  c("OFF-ROUTE", session, "E11_ENABLED && E11_UI_ENABLED", "true"),
  c("DISABLED-NO-IDENTITY", session, "enabled: e11On() && available.data?.enabled === true", "enabled: e11On()"),
  c("IDENTITY-USER", session, "data?.usuarioId === user.id", "!!data"),
  c("IDENTITY-ROLE", session, "data.rolBase === user.rol", "true"),
  c("F-CLIENTS-SALES", page, "path === \"/contabilidad/fiscal\" && fiscal(s)", "path === \"/contabilidad/fiscal\" && false"),
  c("F-NOT-A", page, 's.identity.perfil === "A" && can(s, "FINANZAS_LIMITADAS_LEER")', "true"),
  c("A-NOT-F", page, 's.identity.perfil === "F" && can(s, "FISCAL_LEER")', "true"),
  c("CAPABILITY-FISCAL", page, 's.identity.perfil === "F" && can(s, "FISCAL_LEER")', 's.identity.perfil === "F"'),
  c("CLIENT-PARENT-F", page, ': "/contabilidad/fiscal"} />;', ': "/contabilidad/finanzas"} />;'),
  c("CLIENT-PARENT-A", page, '`/contabilidad/finanzas/clientes/${match[1]}`', '"/contabilidad/finanzas"'),
  c("E5-PARENT-A", page, '${retained[1] ? `/${retained[1]}` : ""}', '${""}'),
  c("E5-PARENT-F", page, "if (retained) return preparation(s) ?", "if (retained) return true ?"),
  c("LEGACY-DEEP-DENIED", page, 'let content: ReactNode = <E11Error error="Ruta no autorizada para este perfil. Usa las proyecciones contables, sin acceso legacy." />;', "let content: ReactNode = <FiscalHome s={s} />;"),
  c("F-DOCUMENT", page, "else if (invoice && fiscal(s)", "else if (invoice && false"),
  c("F-DOCUMENT-ID", page, "document.data.facturaId !== id || document.data.ventaId !== id", "false"),
  c("F-SALES-CLIENT-ID", page, "client && rows.data.items.some(v => v.cliente.clienteId !== client)", "false"),
  c("F-PAGER", page, "onClick={() => setCursor(next ?? undefined)}", "onClick={() => setCursor(undefined)}"),
  c("READ-ERROR-NOT-ZERO", page, "return <>{query.error ?", "return <>{false ?"),
  c("A-CLIENTS", page, "else if (path === \"/contabilidad/finanzas\" && financial(s))", "else if (path === \"/contabilidad/finanzas\" && false)"),
  c("A-NOTES-UNINVOICED", page, "notes.data.items.map(n => <p", "notes.data.items.filter(n => n.facturada).map(n => <p"),
  c("A-ACCOUNT-ID", page, "account.data.cliente.clienteId !== id ?", "false ?"),
  c("A-NOTE-ID", page, "notes.data.items.some(n => n.clienteId !== id)", "false"),
  c("PROFILE-ADMIN", page, "data => mutation.mutateAsync({ usuarioId: id, data })", "data => mutation.mutateAsync({ usuarioId: id, data: { ...data, motivo: \"Sin motivo revisado\" } })"),
  c("PROFILE-NONADMIN", page, "!admin(s) || !can(s, \"PERFILES_ADMINISTRAR\")", "false"),
  c("PROFILE-REASON", page, "disabled={!reason.trim()}", "disabled={false}"),
  c("PROFILE-HISTORY-ID", page, "history.data.items.some(e => e.usuarioId !== id)", "false"),
  c("PROFILE-FRESH", page, "!retry && fresh.data.perfilVersion !== command.revisionEsperada", "false"),
  c("PROFILE-RESPONSE-ID", page, "r.usuarioId === id && r.actorId", "true && r.actorId"),
  c("PERIOD-WEEK-CALENDAR", page, 'if (p.tipo === "SEMANA" && date.getUTCDay() !== 1) return false;', 'if (false) return false;'),
  c("PERIOD-OBLIGATION", page, 'if (p.obligatorio !== (p.tipo !== "DIA")) return false;', "if (false) return false;"),
  c("PERIOD-OPEN", page, ' || p.estado === "ABIERTO"', ""),
  c("SNAPSHOT-SEND", page, "({ totalRevisado: _localOnly, ...data }) => mutation.mutateAsync({ data })", "data => mutation.mutateAsync({ data })"),
  c("SNAPSHOT-FRESH", page, "fresh.data.fuenteRevision !== command.fuenteRevision || fresh.data.totalFacturado !== command.totalRevisado", "false"),
  c("SNAPSHOT-ID", page, "snapshot.data.id !== id ?", "false ?"),
  c("SNAPSHOT-FROZEN", page, "sales.data.fuenteRevision !== snapshot.fuenteRevision || sales.data.totalFacturado !== snapshot.totalFacturado", "false"),
  c("DECISION-EXACT", page, 'outcome === "ACEPTADA" && cents !== e5Cents(snapshot.totalFacturado)', "false"),
  c("DECISION-OBSERVATION", page, '(outcome === "NO_CUADRA" && !observation.trim())', "false"),
  c("DECISION-NO-CUADRA", page, "resultado: outcome, totalExterno", 'resultado: "ACEPTADA" as const, totalExterno'),
  c("DECISION-FRESH", page, "source.fuenteRevision !== command.fuenteRevision", "false"),
  c("ADMIN-NOTICE", page, 'periods.data.items.filter(p => !noticesOnly || p.estado === "NO_CUADRA").map', "periods.data.items.map"),
  c("ADMIN-NOTICE-LINK", page, 'href={`/contabilidad/conciliaciones/${id}`}>Revisar snapshot', 'href="/contabilidad/conciliaciones">Revisar snapshot'),
  c("PREPARATIONS", page, 'if (path === "/contabilidad/preparaciones") content', 'if (false) content'),
  c("PREPARATION-ID", page, "source.data.cobroId !== id || source.data.notas.some(n => n.clienteId !== source.data!.clienteId)", "false"),
  c("PREPARATION-SEND", page, "return { notaId: n.notaId, movimientoVentaId: n.movimientoVentaId, importe: e5Decimal(cents) };", "return { notaId: n.notaId, movimientoVentaId: n.notaId, importe: e5Decimal(cents) };"),
  c("PREPARATION-RETAINED", page, "assignments.reduce((sum, a) => sum + e5Cents(a.importe), 0) > e5Cents(source.retenido)", "false"),
  c("PREPARATION-FRESH", page, "!retry && (fresh.revision !== command.revisionEsperada || fresh.fuenteRevision !== command.fuenteRevision)", "false"),
  c("PREPARATION-RESPONSE", page, "r.retenido === source.retenido &&", "true &&"),
  c("WRITE-DOUBLE", write, "!session || lock.current || settled.current", "!session || settled.current"),
  c("WRITE-RETRY-EXACT", write, "retry: () => saved.current && run(saved.current, true)", "retry: () => saved.current && run({ ...saved.current, uuid: crypto.randomUUID() }, true)"),
  c("WRITE-409-NEW-UUID", write, "if (definitiveFirstRejection)", "if (false)"),
  c("WRITE-REVOKED", session, "e11IdentityKey(fresh) !== key", "false"),
  c("CACHE-PROFILE-VERSION", session, "i.perfil, i.perfilVersion, i.permisosVersion", "i.perfil, i.permisosVersion"),
  c("UNCERTAIN-RETRY-403-PRESERVES", write, "sent && !previouslyUncertain && !special &&", "sent && !special &&"),
  c("UNCERTAIN-RETRY-STALE-PRESERVES", write, "sent && !previouslyUncertain && !special &&", "sent && !special &&"),
  c("POSTCOMMIT-PROFILE-PRIVACY-UUID", recovery, "localStorage.setItem(e11RecoveryKey(record), JSON.stringify(record));", "localStorage.removeItem(e11RecoveryKey(record));"),
  c("APP-COUNTER-BOUNDARY", "src/App.tsx", "<E11ApplicationBoundary><Router /></E11ApplicationBoundary>", "<Router />"),
  c("APP-ADMIN-ROUTE", "src/App.tsx", '{e11On() && <Route path="/contabilidad/conciliaciones" component={E11RoutePage} />}', '{false && <Route path="/contabilidad/conciliaciones" component={E11RoutePage} />}'),
  c("APP-E5-DOCUMENT-PRIVATE", page, 'let content: ReactNode = <E11Error error="Ruta no autorizada para este perfil. Usa las proyecciones contables, sin acceso legacy." />;', "let content: ReactNode = <FiscalHome s={s} />;"),
  c("APP-PAGOS-REDIRECT", page, 'if (path === "/pagos-dirigidos" && preparation(s))', "if (false)"),
  c("USUARIOS-PARENT-CONTROL", "src/pages/usuarios.tsx", "<E11ProfileControl usuarioId={user.id} active={user.activo} />", "<span />"),
  c("USUARIOS-NOT-OTHER-ROLES", "src/pages/usuarios.tsx", "isAdmin && user.rol === Role.CONTADOR && <E11ProfileControl", "isAdmin && <E11ProfileControl"),
  c("USUARIOS-PARENT-ASSIGN", "src/pages/usuarios.tsx", "usuarioId={user.id} active={user.activo}", "usuarioId={11} active={user.activo}"),
  c("NOTIFICACIONES-PARENT", "src/pages/notificaciones.tsx", "<E11SnapshotNoticeLink entity={item.entidad} id={item.entidadId} />", "<span />"),
  c("CONCILIACION-PARENT", "src/pages/conciliacion.tsx", "<E11AdminNotices />", "<span />"),
  c("E5-SCOPE-PROFILE-VERSION", "src/lib/e5-authorization.ts", "e11.perfil, e11.perfilVersion, e11.permisosVersion", "e11.perfil, e11.permisosVersion"),
  c("E5-SCOPE-PERMISSIONS-VERSION", "src/lib/e5-authorization.ts", "e11.perfilVersion, e11.permisosVersion", "e11.perfilVersion"),
  c("E5-LEGACY-CONTADOR-BLOCKED", "src/components/e5-pendientes.tsx", 'if (e11On() && user.data.rol === "CONTADOR")', "if (false)"),
  c("CALENDAR-MONTH-FIRST", page, 'if (p.tipo === "MES" && date.getUTCDate() !== 1) return false;', "if (false) return false;"),
  c("CALENDAR-WEEK-LENGTH", page, "date.toISOString().slice(0, 10) === p.finExclusivo", "true"),
  c("CALENDAR-MONTH-LEAP", page, 'if (p.tipo === "MES") date.setUTCMonth(date.getUTCMonth() + 1);', 'if (p.tipo === "MES") date.setUTCDate(date.getUTCDate() + 30);'),
  c("CALENDAR-DAY-OPTIONAL", page, 'p.obligatorio !== (p.tipo !== "DIA")', 'p.obligatorio !== true'),
  c("CALENDAR-NO-DEADLINE", page, 'if (p.obligatorio !== (p.tipo !== "DIA")) return false;', 'if (p.obligatorio !== (p.tipo !== "DIA") || p.finExclusivo < "2026-01-01") return false;'),
  c("CALENDAR-FUTURE", page, " || p.finExclusivo > todayInMexicoCity()", ""),
  c("CALENDAR-ACCEPTED", page, '!closedPeriod(p) || p.estado === "ACEPTADA"', "!closedPeriod(p)"),
  c("SNAPSHOT-PERIOD-FRESH", page, " || current.ultimaConciliacionId !== selected.ultimaConciliacionId", ""),
  c("SNAPSHOT-RESPONSE-PERIOD", page, "&& samePeriod(r.periodo, period)", "&& true"),
  c("SNAPSHOT-RESPONSE-TOTAL", page, "&& r.totalFacturado === command.totalRevisado", "&& true"),
  c("DECISION-RESPONSE-ACTOR", page, "d.uuid === command.uuid && d.actorId === s.identity.usuarioId", "d.uuid === command.uuid"),
  c("DECISION-HISTORICAL", page, "reconciling(s) && snapshot.vigente && !snapshot.decisiones.length", "reconciling(s) && !snapshot.decisiones.length"),
  c("DECISION-ALREADY-DECIDED", page, "reconciling(s) && snapshot.vigente && !snapshot.decisiones.length", "reconciling(s) && snapshot.vigente"),
  c("PROFILE-RESPONSE-UUID", page, "r.uuid === command.uuid && r.posterior", "true && r.posterior"),
  c("PROFILE-RESPONSE-ACTOR", page, "r.usuarioId === id && r.actorId === s.identity.usuarioId", "r.usuarioId === id"),
  c("PROFILE-RESPONSE-REVISION", page, "r.revision === command.revisionEsperada + 1", "true"),
  c("FISCAL-PRIVATE-FIELDS", page, "<p>{v.cliente.nombre} · {timestamp(v.fechaFacturacion)}", "<p>{JSON.stringify(v)} · {timestamp(v.fechaFacturacion)}"),
  c("A-PRIVATE-FIELDS", page, "{c.contacto} · límite", "{JSON.stringify(c)} · límite"),
  c("A-TO-F-REVOKES-CACHE", session, "client.removeQueries({ predicate: sensitive });", "void 0;"),
  c("FINANCE-NOTES-PAGE", page, "cursor: noteCursor, limit: 20", "cursor: undefined, limit: 20"),
  c("FINANCE-ACCOUNT-PAGE", page, "cursor: movementCursor, limit: 20", "cursor: undefined, limit: 20"),
  c("FISCAL-SALES-PAGE", page, "const params = { ...range, clienteId: client, cursor, limit: 20 };", "const params = { ...range, clienteId: client, cursor: undefined, limit: 20 };"),
  c("SNAPSHOT-SALES-PAGE", page, "api.useListE11ConciliacionVentas(snapshot.id, { cursor, limit: 20 }", "api.useListE11ConciliacionVentas(snapshot.id, { cursor: undefined, limit: 20 }"),
  c("PREPARATIONS-PAGE", page, "const params = { clienteId: client, cursor, limit: 20 };", "const params = { clienteId: client, cursor: undefined, limit: 20 };"),
  c("HISTORY-PAGE", page, "api.useListE11PerfilHistorial(id, { cursor, limit: 10 }", "api.useListE11PerfilHistorial(id, { cursor: undefined, limit: 10 }"),
  c("PAGE-ERROR-RESTART", page, "onClick={restart}>Descartar cursor", "onClick={() => {}}>Descartar cursor"),
  c("IDENTITY-AUTH-REVOKED", session, "if (available.error || identity.error)", "if (available.error)"),
  c("FISCAL-FORBIDDEN-NO-FALLBACK", page, "return <>{query.error ?", "return <>{false ?"),
  c("PERIODS-PAGE", page, "const params = { ...range, cursor, limit: 20 };", "const params = { ...range, cursor: undefined, limit: 20 };"),
  c("FINANCE-CLIENTS-PAGE", page, "const params = { cursor, limit: 20 };", "const params = { cursor: undefined, limit: 20 };"),
  c("PREPARATION-NOTE-BALANCE", page, "if (!cents || cents > e5Cents(n.saldo))", "if (!cents)"),
  c("PREPARATION-EXACT-CHARGE-FRESH", page, "n.notaId === a.notaId && n.movimientoVentaId === a.movimientoVentaId", "n.notaId === a.notaId"),
  c("PREPARATION-NOTES-SAME-CLIENT", page, "source.data.cobroId !== id || source.data.notas.some(n => n.clienteId !== source.data!.clienteId)", "source.data.cobroId !== id"),
  c("PREPARATION-LIST-CLIENT", page, "client && rows.data.items.some(c => c.clienteId !== client)", "false"),
  c("PREPARATION-DEPENDENCY-OFF", page, "&& E11_E5_PREPARATION_ENABLED && s.flags.preparacionE5", "&& E11_E5_PREPARATION_ENABLED"),
  c("PROFILE-DEPENDENCY-OFF", page, "|| !E11_PROFILE_ASSIGNMENT_ENABLED || !s.flags.perfiles", "|| !E11_PROFILE_ASSIGNMENT_ENABLED"),
  c("CONCILIATION-DEPENDENCY-OFF", page, "else if ((fiscal(s) || admin(s)) && E11_RECONCILIATION_ENABLED && s.flags.conciliacion)", "else if ((fiscal(s) || admin(s)) && E11_RECONCILIATION_ENABLED)"),
  c("APP-OFF-USUARIOS", session, "E11_ENABLED && E11_UI_ENABLED", "true"),
  c("APP-ADMIN-NAVIGATION", "src/components/layout/app-layout.tsx", "<E11Navigation />", "<span />"),
  c("ADMIN-BELL-LINK", "src/components/notifications-bell.tsx", "e11?.flags.conciliacion && e11.identity.rolBase", "false && e11.identity.rolBase"),
  c("OFF-BELL-NO-LINK", "src/components/notifications-bell.tsx", 'event.href.startsWith("/contabilidad/") && !e11LinkAllowed', "false"),
  c("E5-LAST-PROFILE-RECHECK", "src/hooks/use-e5-actions.ts", "const lastE11Identity = e11 ? await e11.check() : undefined;", "const lastE11Identity = e11?.identity;"),
  c("E5-LAST-PERMISSIONS-RECHECK", "src/hooks/use-e5-actions.ts", "const lastE11Identity = e11 ? await e11.check() : undefined;", "const lastE11Identity = e11?.identity;"),
  c("E5-OFF-LEGACY-CAPABILITY", "src/lib/e5-authorization.ts", 'if (user.rol === "CONTADOR") return !(E11_ENABLED && E11_UI_ENABLED) && !!caps?.preparacionADisponible && caps.puedePreparar;', 'if (user.rol === "CONTADOR") return false;'),
  c("RECOVERY-ORIGINAL-ACTOR", resolver, "useGetE11OperacionRecuperacion(record.actorId, record.accion, record.uuidOriginal", "useGetE11OperacionRecuperacion(session.identity.usuarioId, record.accion, record.uuidOriginal"),
  c("RECOVERY-ORIGINAL-ACTION", resolver, "useGetE11OperacionRecuperacion(record.actorId, record.accion, record.uuidOriginal", 'useGetE11OperacionRecuperacion(record.actorId, "PERFIL", record.uuidOriginal'),
  c("RECOVERY-ORIGINAL-UUID", resolver, "useGetE11OperacionRecuperacion(record.actorId, record.accion, record.uuidOriginal", 'useGetE11OperacionRecuperacion(record.actorId, record.accion, "22222222-2222-4222-8222-222222222222"'),
  c("RECOVERY-RELOAD", session, 'return () => { window.removeEventListener("e11-recovery", refresh);', 'return () => { for (const k of Object.keys(localStorage)) if (k.startsWith("e11-recovery:")) localStorage.removeItem(k); window.removeEventListener("e11-recovery", refresh);'),
  c("RECOVERY-ADMIN-INDEPENDENT-GATES", resolver, "!session.flags.enabled", "(!session.flags.enabled || !session.flags.conciliacion)"),
  c("RECOVERY-CAPABILITY", resolver, '!session.identity.capacidades.includes("FISCAL_LEER")', "false"),
  c("RECOVERY-NONADMIN", resolver, 'session.identity.rolBase !== "ADMIN"\n    || session.identity.perfil !== null', "false"),
  c("RECOVERY-OFF", resolver, "!E11_ENABLED || !E11_UI_ENABLED", "false"),
  c("RECOVERY-GET-PENDING", resolver, "setMetadata(response);", `setMetadata(response); ${discardMarker}`),
  c("RECOVERY-GET-COMMITTED-UNAUDITED", resolver, "setMetadata(response);", `setMetadata(response); if (response.estado === "CONFIRMADA") { ${discardMarker} }`),
  c("RECOVERY-GET-CONFIRMED", recovery, "localStorage.removeItem(e11RecoveryKey(expected));", "void 0;"),
  c("RECOVERY-GET-TOMBSTONE", resolver, '["CONFIRMADA", "CERRADA_SIN_EFECTO"].includes(response.estado)', '["CONFIRMADA"].includes(response.estado)'),
  c("RECOVERY-GET-404", resolver, "if (query.data && !query.error) void accept(query.data)", `if (query.error) { ${discardMarker} } if (query.data && !query.error) void accept(query.data)`),
  c("RECOVERY-GET-ERROR", resolver, "if (query.data && !query.error) void accept(query.data)", `if (query.error) { ${discardMarker} } if (query.data && !query.error) void accept(query.data)`),
  c("RECOVERY-RESPONSE-ACTOR", recovery, "a.actorId === b.actorId && a.accion === b.accion", "a.accion === b.accion"),
  c("RECOVERY-RESPONSE-ACTION", recovery, "a.actorId === b.actorId && a.accion === b.accion", "a.actorId === b.actorId"),
  c("RECOVERY-RESPONSE-UUID", recovery, "&& a.uuidOriginal === b.uuidOriginal", "&& true"),
  c("RECOVERY-RESPONSE-REVISION", recovery, "|| !/^[a-f0-9]{64}$/.test(r.revision)", "|| false"),
  c("RECOVERY-RESPONSE-RESOLUTION-ID", recovery, "!/^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$/.test(r.resolucionId)", "false"),
  c("RECOVERY-RESPONSE-RESOLVED-AT", recovery, '|| !r.resueltoEn || !Number.isFinite(Date.parse(r.resueltoEn)))\n    || (r.estado', '|| false)\n    || (r.estado'),
  c("RECOVERY-EXACT-RELEASE", recovery, "localStorage.removeItem(e11RecoveryKey(expected));", 'for (const key of Object.keys(localStorage)) if (key.startsWith("e11-recovery:")) localStorage.removeItem(key);'),
  c("RECOVERY-POST-BODY", resolver, "mutation.mutateAsync({ ...target, data: command })", "mutation.mutateAsync({ ...target, data: { ...command, ...target } })"),
  c("RECOVERY-POST-UUID", resolver, "uuid: crypto.randomUUID(), revisionEsperada:", "uuid: record.uuidOriginal, revisionEsperada:"),
  c("RECOVERY-POST-REASON", resolver, "(!body && !reason.trim())", "false"),
  c("RECOVERY-POST-FRESH-IDENTITY", resolver, "const actor = await session.check();", "const actor = session.identity;"),
  c("RECOVERY-POST-FRESH-CAS", resolver, "revisionEsperada: fresh.data.revision", "revisionEsperada: query.data!.revision"),
  c("RECOVERY-POST-DOUBLE", resolver, "if (lock.current || storageBlocked) return;", "if (storageBlocked) return;"),
  c("RECOVERY-POST-TERMINAL", resolver, "await accept(response);", "void response;"),
  c("RECOVERY-POST-PENDING", resolver, "setMetadata(response);", `setMetadata(response); if (pending.current) { ${discardMarker} }`),
  c("RECOVERY-POST-UNCERTAIN", resolver, resolverCatch, `sessionStorage.removeItem(key); ${resolverCatch}`),
  c("RECOVERY-POST-OUTCOME-ERRORS", resolver, resolverCatch, `sessionStorage.removeItem(key); ${resolverCatch}`),
  c("RECOVERY-POST-REPLAY-EXACT", resolver, "let command = pending.current;", "let command = pending.current ? { ...pending.current, uuid: crypto.randomUUID() } : null;"),
  c("RECOVERY-POST-REPLAY-403", resolver, resolverCatch, `sessionStorage.removeItem(key); ${resolverCatch}`),
  c("RECOVERY-POST-CAS-PENDING", resolver, catchConsult, "void 0;"),
  c("RECOVERY-POST-CAS-TERMINAL", resolver, catchConsult, "void 0;"),
  c("RECOVERY-POST-TIMEOUT-GET-CONFIRMED", resolver, catchConsult, "void 0;"),
  c("RECOVERY-POST-TIMEOUT-GET-TOMBSTONE", resolver, catchConsult, "void 0;"),
  c("RECOVERY-NO-RECURSIVE-QUARANTINE", resolver, catchConsult, 'if (pending.current) localStorage.setItem(`e11-recovery:${session.identity.usuarioId}:PERFIL:${pending.current.uuid}`, JSON.stringify({ actorId: session.identity.usuarioId, accion: "PERFIL", uuidOriginal: pending.current.uuid, state: "QUARANTINE" })); ' + catchConsult),
  c("RECOVERY-ADMIN-SESSION-LOSS", session, 'else if (name.startsWith("e11-resolver:") && (oldKey ? name.includes(encodeURIComponent(oldKey)) : !retainKey || !name.includes(encodeURIComponent(retainKey)))) storage.removeItem(name);', 'else if (name.startsWith("e11-resolver:") && (oldKey ? name.includes(encodeURIComponent(oldKey)) : !retainKey || !name.includes(encodeURIComponent(retainKey)))) void 0;'),
  c("RECOVERY-SECOND-ADMIN", session, 'else if (name.startsWith("e11-resolver:") && (oldKey ? name.includes(encodeURIComponent(oldKey)) : !retainKey || !name.includes(encodeURIComponent(retainKey)))) storage.removeItem(name);', 'else if (name.startsWith("e11-resolver:") && (oldKey ? name.includes(encodeURIComponent(oldKey)) : !retainKey || !name.includes(encodeURIComponent(retainKey)))) { const r = JSON.parse(storage.getItem(name)!); localStorage.removeItem(`e11-recovery:${r.target.actorId}:${r.target.accion}:${r.target.uuidOriginal}`); storage.removeItem(name); }'),
  c("RECOVERY-LEGACY-MARKER-NO-GUESS", session, 'catch { setProblem("No se pudo leer el registro de recuperación.', 'catch { for (const k of Object.keys(localStorage)) if (k.startsWith("e11-recovery:")) localStorage.removeItem(k); setProblem("No se pudo leer el registro de recuperación.'),
  c("RECOVERY-NO-BUSINESS-RESEND", write, "if (saved.current && !records.some(r => matches(r, saved.current?.uuid)))", "if (false)"),
];