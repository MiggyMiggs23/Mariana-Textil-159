# E2 — Bloque 0: diagnóstico previo, sin implementación

## Alcance y revisión

- Código inspeccionado: `1ad2cad601a667c586e084accbf52b3ca3b7cef7`.
- Árbol de esa revisión: `9fd771da640ae7b184a85ffc02dda130c885843e`.
- Base solicitada: `80eaa93d4300e86d9be54492e634f88f0c0abc90`; existe y es antecesora de HEAD.
- Solo lectura de código, documentos y metadatos Git. Este informe no autoriza escrituras en ninguna base.
- No se consultó la base, no se ejecutaron pruebas, typecheck, cierres, devoluciones, migraciones ni reinicios. No se crearon usuarios ni sesiones.
- Los cambios previos de otros trabajos se conservaron. No se atribuyen a E2 los resultados de pruebas de entregas anteriores.

## 1. Fórmula actual

No se encontró un símbolo `cuadrarCaja` en las fuentes actuales de frontend/backend. Sus responsabilidades están en:

- `artifacts/api-server/src/lib/pos.ts:2213`: `buildCorteCaja`.
- Fórmula exacta en `pos.ts:2506`: fondo inicial + pagos de tickets en efectivo − salidas de CAJA_FISICA.
- La sesión aporta el fondo; pagos de tickets vendidos aportan efectivo; `salidas_dinero_caja` aporta las salidas de la sesión. Lecturas en `pos.ts:2247-2268`, acumulación en `2452-2506`.
- El historial vuelve a calcularla en `listarSesionesCajaHistorial`, `pos.ts:2153-2208`; fórmula en `2195-2198`.
- Hay una tercera versión en `artifacts/api-server/src/lib/admin-analytics.ts:1225-1264`: fondo + efectivo de tickets, **sin restar salidas**; afecta también diferencia, filtro de diferencias y totales.

Ninguna de estas fórmulas incluye abonos físicos de crédito ni cobros retenidos. La divergencia administrativa es un hallazgo de código, no una cuantificación de diferencias en cortes reales.

## 2. Relación abono–sesión después de E1

La relación estructural **ya existe**; no hay que inventarla desde una nota:

- `lib/db/src/schema/pos.ts:357-406`: movimientos de crédito con naturaleza, sitio, sesión, productor y clave de operación; FK de sesión.
- `lib/db/src/schema/pos.ts:322-349`: entidad independiente `cobros_credito_pendientes_e1`, con sesión, origen, importe, medio y operación. No equivale a un ticket pendiente de cobro.
- `artifacts/api-server/src/lib/credit-evidence-contract.ts:91-105`: el efectivo físico exige CAJA_FISICA y sesión; transferencia exige cuenta bancaria y no sesión; corrección contable no imputa sesión.
- **Ya hay validación y candado de sesión en la aplicación:** `assertCreditEvidenceScope`, `artifacts/api-server/src/lib/credit-evidence.ts:25-36`, toma `FOR SHARE` y comprueba mismo sitio, ABIERTA y ausencia de fecha de cierre.
- La ruta de abono lo llama dentro de transacción (`routes/clientes.ts:2194`); también la ruta de reverso (`:2350`) y pagos dirigidos (`routes/pagos-dirigidos.ts:328,383`).

La base de E1 permite identificar la recepción sin multiplicarla por sus aplicaciones. Falta incorporarla al corte y representar la devolución y su salida de forma coordinada. No se ha demostrado todavía el comportamiento transaccional de E2 ni su cobertura SQL directa.

## 3. Consumidores y conservación histórica

| Superficie | Fuente actual |
|---|---|
| Cierre | `cerrarSesionCaja` → `buildCorteCaja`, `lib/pos.ts:2614-2667` |
| Corte actual/detalle | `routes/pos.ts:942-951,1012,1060` → constructor de corte |
| Historial de caja | `listarSesionesCajaHistorial`, `lib/pos.ts:2153-2210` |
| Listado administrativo, diferencias y totales | SQL independiente de `lib/admin-analytics.ts:1225-1284` |
| Caja/Cortes y exportación | `artifacts/mariana-textil/src/pages/caja/cortes.tsx:101-168,410-425` |
| Detalle/papel | `src/pages/corte-detail-shared.tsx`; reutilizado por `src/pages/cobros.tsx:530-539,1934-1947` |
| Reportes generales/diarios | Familia `routes/reportes.ts:50-60,121-128`; no se identificó una lectura del esperado de sesión en esa familia |

No se debe convertir un reporte de Ventas/Contado/Cobranza en un reporte del cajón. Los consumidores que sí exponen efectivo esperado deben compartir un único cálculo y desglose documental; los agregados financieros excluidos deben permanecer intactos.

**Riesgo principal:** los lectores reconstruyen el esperado también para sesiones cerradas. Cambiar indiscriminadamente la fórmula puede cambiar su valor mostrado aunque no se ejecute UPDATE sobre el corte.

Antes de aplicar E2 hace falta conservar y comparar los valores de todas las superficies históricas. Si existen discrepancias previas entre el administrativo y el detalle, no se corregirán silenciosamente en históricos para forzar una función única. Se reportarán antes de continuar. No se han leído ni comparado importes reales en este Bloque 0.

## 4. Concurrencia con cierre

`cerrarSesionCaja` bloquea la sesión con `FOR UPDATE`, verifica ABIERTA, cierra y construye el corte dentro de la misma transacción (`lib/pos.ts:2630-2667`).

El `FOR SHARE` de `assertCreditEvidenceScope` **sí entra en conflicto** con ese `FOR UPDATE`. No es correcto concluir que E1 carece de coordinación de sesión por mirar solo el INSERT:

- Si la captura obtiene primero el candado compartido dentro de su transacción, el cierre espera su terminación.
- Si el cierre confirma primero, la posterior comprobación de sesión debe rechazarla como cerrada.

Esto es análisis estático de los candados, no una prueba ejecutada. E2 debe conservar esta coordinación durante todos los efectos del abono/devolución, incluida la salida, y demostrar el orden de candados, atomicidad, reintentos y resultado final del cierre concurrente.

No se crearán sesiones ni usuarios de prueba. Los ensayos con escrituras quedan pendientes de un alcance expresamente autorizado que respete esa restricción; no se presupone permiso por tratarse de una base aislada.

## 5. Devolución existente y lo que falta

Existe `POST /clientes/:id/pagos/:pagoId/reversar`, en `artifacts/api-server/src/routes/clientes.ts:2316-2391`.

Ya contiene motivo, importe completo, evidencia E1, clave de operación/idempotencia, candado de crédito, bloqueo del abono y prevención de reverso duplicado. No es todavía la devolución de E2:

- Autoriza por permiso `clientes_finanzas.autorizar`, no por una condición exclusiva ADMIN.
- Lee aplicaciones y las audita, pero no rechaza el abono por haber sido aplicado total o parcialmente (`:2368-2370`).
- No registra una salida física coordinada de la caja actual.
- No se identificó una interfaz de devolución de clientes ni la devolución de cobro retenido.

No debe reutilizarse el reverso sin incorporar los límites E2 ni confundirse una corrección con dinero devuelto. Tampoco debe deducirse “sin aplicar” exclusivamente de la ausencia de filas de evidencia: habrá que comprobar el estado económico vigente sin modificar FIFO/proyección.

## Bloqueo operativo previo a implementar

La restricción de conservar las tres guardas tiene una consecuencia concreta:

- `artifacts/api-server/src/lib/credit-evidence-contract.ts:84-88` bloquea tanto INGRESO_FISICO como DEVOLUCION_FISICA en efectivo.
- `reports/e1-guardas-temporales-2026-09-18/sql/01-install-cash.sql:9-14` hace lo mismo en SQL, con E1C01.
- Pendientes y atribuciones tienen guardas separadas E1P01/E1A01. La instalación y sus rechazos constan en los informes E1; no se volvieron a verificar contra la base hoy.

Por tanto, **mantener esas guardas intactas no permite activar ahora la devolución física en efectivo del crédito**. No se sortearán registrándola como corrección, usando una tabla paralela para eludirlas ni quitando flags/triggers.

Hace falta confirmar si E2 debe preparar la devolución pero mantenerla inhabilitada hasta una autorización posterior, dejando explícitamente pendiente su activación y aceptación operativa. No se replantean las reglas de importe completo, no aplicado, ADMIN, motivo ni conservación del corte original.

## Siguiente punto de control

Antes de cualquier escritura en base se presentará su alcance concreto y se esperará autorización textual, guardada en `reports/`. Este informe no solicita ni presupone una autorización general de DDL/DML.

La verificación futura comparará la suite pertinente contra `80eaa93d`, demostrará el fallo significativo de cada prueba nueva y ejecutará el typecheck completo sobre la revisión final exacta. Nada de eso se declara ejecutado en este Bloque 0.