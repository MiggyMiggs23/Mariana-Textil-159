// Independent, closed code gates. No environment variable or startup migration.
// Release requires review of the documented blockers, not a runtime toggle.
export const REMATE_RELEASED: boolean = false;
export const PRICE_FLOOR_RELEASED: boolean = false;
export const PRODUCT_HISTORY_DELETE_RELEASED: boolean = false;