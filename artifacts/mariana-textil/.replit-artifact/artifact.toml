kind = "web"
previewPath = "/"
title = "Mariana Textil"
description = "Sistema interno para la operación de tiendas y bodegas de Mariana Textil, con autenticación, roles, ubicaciones y administración inicial."
version = "1.0.0"
id = "artifacts/mariana-textil"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20329

[services.development]
run = "cd /home/runner/workspace/artifacts/mariana-textil && exec pnpm exec vite preview --config vite.config.ts --outDir dist-night-task12 --host 0.0.0.0 --port \"$PORT\" --strictPort"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mariana-textil", "run", "build" ]
serve = "static"
publicDir = "artifacts/mariana-textil/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20329"
BASE_PATH = "/"
