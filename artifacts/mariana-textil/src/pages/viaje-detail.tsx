import { Link, useRoute } from "wouter"; import { useGetViaje } from "@workspace/api-client-react"; import { AppLayout } from "@/components/layout/app-layout"; import { Button } from "@/components/ui/button"; import { ArrowLeft, Printer } from "lucide-react"; import { formatNumber } from "@workspace/number-format";
export default function ViajeDetail() { const [, p] = useRoute("/viajes/:id"); const id = Number(p?.id); const { data: rawViaje } = useGetViaje(id); const viaje: any = rawViaje; if (!viaje) return <AppLayout><p>Cargando viaje…</p></AppLayout>; return <AppLayout><div className="mx-auto max-w-5xl space-y-6"><div className="flex justify-between"><Link href="/viajes"><Button variant="ghost"><ArrowLeft />Volver</Button></Link><Link href={`/viajes/${id}/documento`}><Button><Printer />Imprimir hoja</Button></Link></div><section><h1 className="text-3xl font-bold">Viaje {viaje.folioFormateado}</h1><p>{viaje.nombreOrigen} · {new Date(viaje.salidaAt).toLocaleString("es-MX")} · {viaje.camioneta} · {viaje.chofer}</p></section><div className="grid grid-cols-3 gap-3"><div className="rounded border p-3">{viaje.documentos} documentos</div><div className="rounded border p-3">{formatNumber(viaje.totalRollos,{kind:"count"})} rollos</div><div className="rounded border p-3">{viaje.totalMetros} m · {viaje.totalKilos} kg</div></div><p><b>Destinos:</b> {viaje.destinos.join(", ")}</p><section className="rounded border p-4"><h2 className="font-bold">Documentos</h2>{viaje.tickets.map((x: any) => <Link className="block py-2 underline" href={`/tickets/${x.id}`} key={`t${x.id}`}>Nota #{x.folio} · {x.destinatario || x.cliente}</Link>)}{viaje.salidas.map((x: any) => <Link className="block py-2 underline" href={`/salidas/${x.id}`} key={`s${x.id}`}>Salida #{x.folio} · {x.destino}</Link>)}</section><section className="rounded border p-4"><h2 className="font-bold">Control por rollo</h2>{viaje.rollos.map((x: any,i: number) => <p key={`${x.documento}${i}`} className="font-mono">{x.serie} · {x.tela} {x.color} · {x.cantidad} {x.unidad}</p>)}</section></div></AppLayout>; }